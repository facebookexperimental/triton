# Self Check

This package contains deterministic regression tests for the optimization system. It is
not an autonomous agent and has no authority over optimization outcomes.

Run it after changes to orchestration, contracts, profiling, source handling, VCS,
target routing, or directory structure. Report exact commands, pass/fail counts, failing
test names, and relevant diagnostics. Never weaken production behavior to obtain a pass.
