# Superseded v7 ablations

The artifacts in this directory are retained only as an audit trail. They were
produced before the P0 fidelity fixes and are not paper-comparable. Use
`run_ablations.sh`, which writes a new `ablations_v8` directory by default.
